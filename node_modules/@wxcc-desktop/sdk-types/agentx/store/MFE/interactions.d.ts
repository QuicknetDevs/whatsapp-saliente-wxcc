import { InteractionsServiceTypes } from "@agentx/agentx-services";
export default class ModuleInteractions {
    initialCompletedTimeRange?: InteractionsServiceTypes.TimeRange;
    constructor();
    setInitialCompletedTimeRange(timeRange?: InteractionsServiceTypes.TimeRange): void;
}
